{
  "buildCommand": "cd frontend && npm install --legacy-peer-deps && npm install ajv@8 --save-dev --legacy-peer-deps && CI=false npm run build",
  "outputDirectory": "frontend/build",
  "installCommand": "cd frontend && npm install --legacy-peer-deps && npm install ajv@8 --save-dev --legacy-peer-deps",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}